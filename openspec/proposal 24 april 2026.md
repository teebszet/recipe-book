problem: there is too much friction with adding more recipes

why: this is a problem because there are not enough recipes for the app to be useful yet. requires manual input which is slow

potential direction: often i receive recipes shared with me as links. sometimes website urls, sometimes social urls like instagram. knowing that some saas recipe book apps out there also have an 'import from instagram' feature, i want to research that and create a plan to implement the feature. the feature should also import from a vanilla recipe url too.

unknowns: if an instagram recipe has details in the video, how do we extract the necessary inputs from that? do we need transcript? how do we extract at least 1 photo from the recipe? is it a screenshot of the video? these could be edge cases which don't need immediate attention, but need to determine if it's an edge case, or common.

some test cases: look up 'kenzoeats' IG account and pick some toddler recipes

task 2: rename the app from 'Recipe book' to 'James eats'